{% extends 'base.html.twig' %}

{% block title %}
	Page de
	{{product.nom}}
{% endblock %}

{% block body %}
	<nav aria-label="breadcrumb">
		<ol class="breadcrumb">
			<li class="breadcrumb-item">
				<a href="{{path('homepage')}}">Accueil</a>
			</li>
			<li class="breadcrumb-item">
				<a href="{{path('product_category' ,{'slug': product.category.slug})}}">{{product.category.nom}}</a>
			</li>
			<li class="breadcrumb-item active" aria-current="page">{{product.nom}}</li>
		</ol>
	</nav>
	<h1>{{product.nom}}
		({{product.prix}}
		€)</h1>
	<div class="row">
		<div class="col-4">
			<img src="{{product.image}}" alt="{{product.nom}}" class="img-fluid">
		</div>
		<div class="col">
			<span class="badge bg-primary">
				{{product.category.nom}}
			</span>
			<p>{{product.designation}}</p>
			<a href="#" class="btn btn-success btn-lg m-auto text-center">
				<i class="fas fa-cart-plus"></i>
				Ajouter au panier
			</a>
		</div>
	</div>
{% endblock %}
